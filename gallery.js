function showPic(picObject) {
            var pic = picObject;
            var img = document.getElementById('gm');
            img.setAttribute('src', pic);
            currentImage = pic;
        }
